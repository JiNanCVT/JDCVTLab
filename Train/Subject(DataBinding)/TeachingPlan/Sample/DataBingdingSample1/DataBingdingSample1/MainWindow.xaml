<Window x:Class="DataBindingSample1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:my ="clr-namespace:DataBindingSample1"
        Title="演示省、市数据绑定" Height="350" Width="525">
    <Window.Resources>
        <my:VMProvinces x:Key="Provinces"/>
    </Window.Resources>
    <Grid Name="Gid_ProvinceCity" DataContext="{Binding Source={StaticResource Provinces}, Path=Provinces}" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding}"   DisplayMemberPath="Name" Margin="1,1,0,0"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <ListBox Grid.Column="1" ItemsSource="{Binding Path= Cities }" DisplayMemberPath="Name" Margin="1,1,0,0"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</Window>
